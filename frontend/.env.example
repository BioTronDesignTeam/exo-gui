# Copy to frontend/.env.local (gitignored) to override.
# OAuthManager — login, guest key, session.
VITE_AUTH_URL="http://localhost:18080"
# exo-gui backend — telemetry API (not auth).
VITE_API_URL="http://localhost:8080"
