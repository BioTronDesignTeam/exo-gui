# Copy to prisma/.env (gitignored) for local use outside the dev container.
# Inside the dev container this is provided by docker-compose.
DATABASE_URL="postgresql://exo:exo@db:5432/exo?schema=public"
