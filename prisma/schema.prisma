datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// Models deferred until the telemetry schema is decided — see root CLAUDE.md.
